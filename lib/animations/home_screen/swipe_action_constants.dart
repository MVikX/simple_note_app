import 'package:flutter/material.dart';

// animation settings
const swipeDismissDuration = Duration(milliseconds: 700);

const swipeDismissOffset = Offset(-2.0, 0.0);
const swipeVelocityThreshold = 250.0;

// animation scale values
const scaleStart = 1.0;
const scaleEnd = 0.5;

// animation opacity values
const opacityStart = 1.0;
const opacityEnd = 0.0;

// clipper values
const bezierMultiplier = 1.2;
const clipVerticalCenter = 0.5;

// clipping base values
const clipStartX = 0.0;
const clipStartY = 0.0;
const clipEndFactor = 1.0;