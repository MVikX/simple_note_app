import 'package:flutter/material.dart';

const double noteDefaultPadding = 16.0;
const double noteCardBorderRadius = 16.0;

const double noteFontSize = 16.0;
const double noteLineHeight = 1.5;

const double noteShadowBlurRadius = 10.0;
const Offset noteShadowOffset = Offset(0, 5);
const Color noteShadowColor = Color.fromRGBO(0, 0, 0, 0.05);

const Duration noteSnackDuration = Duration(seconds: 1);